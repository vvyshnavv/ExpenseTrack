# Copy to .env locally. On Render, add these in Dashboard → Environment.

MONGO_URI=mongodb+srv://USER:PASS@cluster.mongodb.net/expense-track?retryWrites=true&w=majority
JWT_SECRET=use-a-long-random-string-at-least-32-chars
PORT=3000
