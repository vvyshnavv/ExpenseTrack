# Optional: backend URL when the API is not on the same origin (e.g. Vercel frontend + Render API).
# Leave unset for local dev (Vite proxies /api to the Node server).
# Example (no trailing slash):
# VITE_API_URL=https://expense-api.onrender.com

VITE_API_URL=
